# 贡献指南（CONTRIBUTING）

感谢你对 **tongming-portkit**（RISC-V 代码移植工具包）的关注与贡献。本文档说明如何参与本项目、提交代码与文档，以及贡献过程中的规范要求。请在提交前完整阅读本指南。

> 仓库地址：GitHub / Gitee（镜像同步）　｜　开源协议：MulanPSL-2.0　｜　默认分支：main

---

## 目录

- [一、参与方式与行为准则](#一参与方式与行为准则)
- [二、开发环境准备](#二开发环境准备)
- [三、分支管理规范](#三分支管理规范)
- [四、贡献者提交要求](#四贡献者提交要求)
- [五、提交信息规范](#五提交信息规范)
- [六、编码规范](#六编码规范)
- [七、测试与文档要求](#七测试与文档要求)
- [八、Pull Request 审核与合并](#八pull-request-审核与合并)
- [九、贡献者授权](#九贡献者授权cla--dco)
- [十、许可与知识产权](#十一许可与知识产权)
- [十](#十二常见问题)[一](#十一许可与知识产权)、常见问题

---

## 一、参与方式与行为准则

本项目欢迎以下形式的贡献：

- 报告缺陷、提出功能建议（提交 Issue）
- 修复缺陷、补充测试用例
- 完善文档、使用示例与示例工程
- 参与代码评审、讨论技术方案
- 贡献与 RISC-V 代码移植相关的规则、规则库与适配能力

参与本项目即表示你同意遵守社区的基本行为准则：

- 尊重所有参与者，不进行人身攻击、歧视、骚扰或贬损性评价；
- 讨论聚焦技术本身，就事论事、有理有据；
- 不发布侵权、违法、恶意或不当内容；
- 不讨论、不提交绕过授权、破解、抓取受保护数据等违规用法；
- 维护者在社区矛盾出现时有权进行提醒、引导与必要的处置。

## 二、开发环境准备

1. Fork 本仓库到你的个人账号，然后克隆你的 Fork 到本地：

   ```bash
   git clone https://github.com/<your-account>/tongming-portkit.git
   cd tongming-portkit
   git remote add upstream https://github.com/rdi-bj/tongming-portkit.git
   ```
2. 在提交任何代码前，请确认：

   - 已配置 Git 用户名与邮箱（建议使用与贡献者授权一致的身份）；
   - 本地默认分支已同步上游最新代码；
   - 依赖安装完整，项目可在本地正常构建与运行；
   - 现有测试用例能够通过（具体命令以仓库 README 为准）。
3. 本项目建议的运行与构建环境，请参考 `README.md` 中"快速开始"章节；环境相关的说明如有变更，请同步更新文档。

## 三、分支管理规范

本项目采用 **主干开发 + 短期特性分支** 模式，禁止在 `main` 上直接提交。所有分支命名统一使用 **小写 + 短横线** 格式。

| 分支类型        | 命名示例           | 来源分支                | 合并目标                | 生命周期与要求                                       |
| --------------- | ------------------ | ----------------------- | ----------------------- | ---------------------------------------------------- |
| 主干 main       | `main`           | —                      | —                      | 长期分支，受保护；仅通过 PR 合入，始终保持可发布状态 |
| 开发 develop    | `develop`        | `main`                | `main`                | 长期集成分支，用于版本内集成（可选，视团队规模启用） |
| 功能 feature    | `feature/xxx`    | `main` 或 `develop` | `develop` / `main`  | 短期分支，功能合入后删除                             |
| 修复 fix        | `fix/issue-123`  | `main` 或 `develop` | 对应目标分支            | 短期分支，缺陷修复后删除，PR 中关联 issue 编号       |
| 发布 release    | `release/v1.2.0` | `develop`             | `main` 与 `develop` | 版本冻结期内仅修缺陷，发布后打 Tag 并删除            |
| 紧急修复 hotfix | `hotfix/v1.2.1`  | `main`                | `main` 与 `develop` | 用于线上问题与安全补丁，优先级最高，修复后删除       |

分支管理补充要求：

- `main` 已启用分支保护：禁止强制推送、要求至少 1 名维护者审核、要求分支与主干同步后再合入；
- 外部贡献者应在自己的 Fork 中创建分支，不在主仓库直接创建分支；
- 提交前先同步主干最新代码，冲突在本地解决后再提交 PR，保持提交历史整洁；
- 已合并或废弃的分支应及时清理，避免分支堆积与权限扩散。

## 四、贡献者提交要求

1. 提交 PR 前，先创建一个 Issue 说明问题或需求；涉及重大改动时，先在 Issue 中讨论方案，达成一致后再实现。
2. PR 描述需说明 **改动背景、实现方案、影响范围、测试情况**，并关联对应的 Issue 编号。
3. 遵循项目编码规范与提交信息规范，单次 PR 聚焦单一主题，避免夹杂无关改动。
4. 新增或修改代码时，需同步补充或更新对应的测试用例与文档。
5. 签署项目要求的贡献者协议（CLA），或按照 DCO 在提交信息中添加签名，确认贡献代码的授权与原创性（详见 [第九节](#九贡献者授权cla--dco)）。
6. 确保 PR 通过 CI 检查（构建、测试、静态检查等）后再请求审核；未通过时请先修复本地问题。

## 五、提交信息规范

提交信息采用 **类型（范围）：简述** 的形式，便于自动生成变更记录与版本说明。

建议格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

类型说明：

| 类型         | 用途                         |
| ------------ | ---------------------------- |
| `feat`     | 新增功能                     |
| `fix`      | 修复缺陷                     |
| `docs`     | 仅文档变更                   |
| `style`    | 代码格式调整，不影响逻辑     |
| `refactor` | 重构，不新增功能也不修复缺陷 |
| `perf`     | 性能优化                     |
| `test`     | 补充或修正测试               |
| `chore`    | 构建、依赖、脚本等杂项变更   |
| `security` | 安全相关修复                 |

示例：

```
fix(parser): 修复宏定义识别中边界条件错误

当宏名紧接数字时，词法分析会越界读取，现改为先校验字符类别。

Closes #123
Signed-off-by: Your Name <your.email@example.com>
```

要求：

- 标题不超过 72 个字符，使用中文或英文均可，句末不加句号；
- 一个提交只做一件事，便于回退与审查；
- 涉及 Issue 时在 footer 中标注 `Closes #编号` 或 `Refs #编号`；
- 按照 DCO 要求签名时，使用 `git commit -s` 自动添加 `Signed-off-by` 行。

## 六、编码规范

- 代码风格以仓库中已有实现为基准，新增代码保持与周边代码风格一致；
- 命名清晰、语义明确，避免无意义的缩写与单字母变量；
- 函数与模块保持单一职责，控制复杂度与嵌套层级；
- 对外暴露的接口须有必要的说明（注释或文档），内部逻辑在关键处补充注释；
- 不提交调试残留（如 `print`、`console.log`、断点、临时文件）；
- 不提交密钥、凭证、内部配置、用户数据、构建产物与大文件；相关忽略规则见仓库 `.gitignore`；
- 删除、修改公共接口或既有行为前，先通过 Issue 讨论兼容性与迁移方案；
- 涉及性能、兼容性或安全的关键改动，请在 PR 中说明评估依据。

## 七、测试与文档要求

- 新增功能应配套新增测试；修复缺陷应补充复现用例或回归测试；
- 关键路径变更必须附带测试用例，并在 CI 通过后方可合并；
- 修改命令行参数、配置文件、公共接口时，同步更新 `README.md` 与相关文档；
- 涉及用户可感知的行为变更，同步在 `CHANGELOG.md` 中记录；
- 涉及依赖变更、权限、文件与网络操作的改动，需在 PR 中特别说明安全风险与评估结论。

## 八、Pull Request 审核与合并

### 审核流程

| 环节     | 审核要点                                                                               | 处理方式                     |
| -------- | -------------------------------------------------------------------------------------- | ---------------------------- |
| 代码审核 | 功能正确性、设计合理性、是否影响兼容性与性能、是否引入后门或恶意行为、是否涉及敏感信息 | 提出修改意见，作者更新后复审 |
| 安全审核 | 涉及依赖变更、权限、文件与网络操作的改动重点审查                                       | 存在风险的按漏洞流程处理     |
| 合并决策 | 至少 1-2 名维护者批准，且无未解决意见                                                  | 由维护者执行合并             |

### 合并与后续

- 合并后自动关闭关联的 Issue，更新 `CHANGELOG.md`，必要时发布新版本；
- 长期未处理（超过约定 SLA）的 PR，维护者需说明当前状态；
- 与项目方向不符的 PR，应礼貌关闭并说明理由；
- 对持续高质量贡献者，经内部审批后可逐步提升权限；
- **管理员权限不授予外部非公司人员**，外部贡献者通过 PR 参与，权限提升须走审批流程。

## 九、贡献者授权

为保障项目知识产权清晰、许可证可长期维护，本项目要求所有外部贡献者确认代码授权。

### 签署 CLA

首次提交 PR 时，请按提示完成签署。签署后即视为你同意：

- 你拥有所提交代码的合法权利，或已获得相应授权；
- 你以与本项目一致的许可证（MulanPSL-2.0）授予项目使用、修改与分发的权利；
- 你确认贡献内容不侵犯第三方知识产权。

## 十、许可与知识产权

- 本项目以 **MulanPSL-2.0** 协议开源，协议全文见仓库 `LICENSE` 文件；
- 所有贡献内容须与 MulanPSL-2.0 兼容；新增第三方依赖时，须检查许可证兼容性；
- 项目的知识产权归属与商标使用规则，以 `LICENSE` 与项目官方说明为准；
- 本次开源范围以仓库说明与 `README.md` 中的范围声明为准；不在开源范围内的模块（如 RBAC 权限管理体系、知识库维护功能、RISC-V 垂域代码大模型软件等）不得以任何形式混入本仓库。

## 十一、常见问题

**Q：第一次贡献应该从哪里开始？**
建议从仓库中标注为 `good first issue` 或 `help wanted` 的 Issue 入手，或先提交文档、测试用例类的改动熟悉流程。

**Q：我的 PR 迟迟没有被审核怎么办？**
可在原 PR 中友善地提醒维护者，并说明当前状态。如超过约定 SLA，维护者应回复处理进度；请勿重复开 PR。

**Q：提交后发现漏了文件或写错了提交信息？**
在对方审核前可追加提交并 `git rebase` 整理；已审核或已合并的提交不要强制推送覆盖历史，应通过新的提交修正。

**Q：我的代码包含了公司的内部逻辑，能否贡献？**
仅可贡献你有权授权、且属于本次开源范围的内容；涉及内部实现、密钥、内部接口的部分须先行剥离。

---

再次感谢你的贡献。如有任何疑问，欢迎通过 Issue 或项目交流渠道与我们沟通。
